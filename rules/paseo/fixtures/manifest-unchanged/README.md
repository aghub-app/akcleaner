# Paseo matching manifest hashes

The synthetic skill directory is deliberately named `worktree-review`, not `paseo`. Its v1 manifest lists two files whose byte hashes match. Both listed files are expected as separate confirmed/unchanged findings. `user-notes.txt` is extra and absent from the manifest, so it must not be attributed to Paseo.
