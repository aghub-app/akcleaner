# Paseo manifest traversal entry

This v1 manifest contains a valid `SKILL.md` entry and a synthetic `../outside.txt` entry. Report the valid file normally, report an invalid manifest path diagnostic for the traversal entry, and do not read or report the neighboring file outside this skill directory.
