# Superset SKILL.md marker

The `superset` directory name alone is not evidence: its synthetic `SKILL.md` lacks the managed marker and must not be found. A different directory named `custom-helper` has the exact marker and is confirmed for its `SKILL.md` only, with unknown integrity. No other file in that directory is attributed.
